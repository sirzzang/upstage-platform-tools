# MLOps Dashboard 활용 질문 가이드

`mlops_dashboard`를 최대한 활용하기 위한 질문 예시들을 카테고리별로 정리했다.

## 더 알아보기

실제 사용 사례와 성능 감상은 [EXAMPLES.md](./EXAMPLES.md)를 참고한다.

## 기본 조회 질문

### 사용자 및 프로젝트
- "김민준의 프로젝트 목록 보여줘"
- "이서연이 담당하는 프로젝트는 뭐야?"
- "Vision AI 팀의 모든 프로젝트 알려줘"
- "active 상태인 프로젝트는 몇 개야?"
- "object_detection 타입의 프로젝트 목록 보여줘"

### 실험 및 파이프라인
- "김민준이 몇 번의 실험을 했어?"
- "yolov8-wafer-exp02 파이프라인의 상태는?"
- "현재 실행 중인 파이프라인은 뭐야?"
- "실패한 파이프라인 목록 보여줘"
- "A100 GPU를 사용한 파이프라인은 몇 개야?"
- "ultralytics 프레임워크로 실행된 실험들 알려줘"

### 모델 정보
- "김민준이 생성한 모델에 대해 알려줘"
- "production 단계에 있는 모델 목록 보여줘"
- "archived된 모델은 어떤 것들이 있어?"
- "YOLOv8 아키텍처를 사용한 모델들 보여줘"
- "파라미터가 30M 이상인 모델은?"

## 성능 메트릭 분석

### 기본 성능 지표
- "mAP50이 가장 높은 모델은 뭐야?"
- "F1 score가 0.9 이상인 모델 목록 보여줘"
- "추론 시간이 가장 빠른 모델은?"
- "precision이 가장 높은 모델 알려줘"
- "recall이 0.95 이상인 모델은?"

### 실무 의사결정 (Precision/Recall 트레이드오프)
- "오탐이 적은 모델 추천해줘" (high precision)
- "미탐이 적은 모델 추천해줘" (high recall)
- "precision과 recall이 균형잡힌 모델은?"
- "confidence threshold가 0.3 이하인 모델들 알려줘"
- "deploy_note에 '오탐 민감'이라고 적힌 모델은?"
- "미탐 민감 현장에 적합한 모델 추천해줘"

### 비교 분석
- "wafer-defect 프로젝트의 모델들을 성능 순으로 비교해줘"
- "같은 프로젝트 내에서 가장 좋은 성능을 낸 모델은?"
- "v2.0과 v2.1 버전의 성능 차이는?"
- "YOLOv8m과 YOLOv5l 모델의 성능 비교해줘"

## 데이터셋 및 아티팩트

### 데이터셋 정보
- "defect-detection-v2 프로젝트의 데이터셋은 뭐야?"
- "COCO 포맷 데이터셋은 몇 개야?"
- "샘플 수가 10000개 이상인 데이터셋 목록 보여줘"
- "가장 큰 데이터셋의 용량은?"

### 아티팩트 조회
- "yolov8-wafer-exp01의 체크포인트 경로 알려줘"
- "defect-detection 파이프라인의 아티팩트 경로 알려줘"
- "로그 파일이 있는 파이프라인은?"
- "config 파일이 저장된 경로는?"
- "체크포인트 파일 크기가 100MB 이상인 것은?"

## 고급 분석 질문

### 통계 및 집계
- "각 사용자별로 생성한 모델 수를 알려줘"
- "프로젝트별 평균 데이터셋 크기는?"
- "팀별로 몇 개의 프로젝트를 진행했어?"
- "월별로 등록된 모델 수는?"
- "task_type별 프로젝트 개수는?"

### 시간 기반 분석
- "최근 3개월 내에 등록된 모델은?"
- "가장 오래된 프로젝트는?"
- "2024년 6월에 완료된 파이프라인은?"
- "학습 시간이 가장 오래 걸린 파이프라인은?"

### 관계형 조회
- "wafer-defect-yolov8m 모델이 사용한 데이터셋은?"
- "surface-inspection 프로젝트의 전체 워크플로우 보여줘"
- "김민준의 프로젝트에서 사용된 모든 데이터셋 알려줘"
- "특정 모델과 연결된 파이프라인, 데이터셋, 프로젝트 정보 모두 보여줘"

## **실무 시나리오 기반** 질문

### 모델 배포 결정
- "불량 유출을 방지해야 하는 현장에 적합한 모델은?" (high recall)
- "불필요한 작업 중단을 피해야 하는 현장용 모델 추천해줘" (high precision)
- "A/B 테스트 중인 모델은?"
- "배포 가능한 모델 중 성능이 가장 좋은 것은?"

### 실험 관리
- "같은 데이터셋으로 여러 실험을 한 경우는?"
- "실험 실패 원인을 확인할 수 있는 로그 경로는?"
- "중간 체크포인트가 있는 실행 중인 파이프라인은?"
- "비슷한 설정으로 실행된 실험들을 비교해줘"

### 리소스 관리
- "H100 GPU를 사용하는 파이프라인은?"
- "가장 많은 GPU 리소스를 사용한 프로젝트는?"
- "체크포인트 파일들의 총 용량은?"

## 복합 질문 (여러 조건 조합)

- "Vision AI 팀에서 production 단계에 있는 모델 중 F1 score가 0.9 이상인 것은?"
- "object_detection 타입 프로젝트에서 mAP50이 0.9 이상이고 추론 시간이 15ms 이하인 모델은?"
- "김민준이 만든 모델 중 precision이 높고 오탐 민감 현장에 적합한 것은?"
- "2024년 6월 이후에 등록된 모델 중 recall이 가장 높은 것은?"

